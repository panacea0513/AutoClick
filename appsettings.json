{
  "Settings": {
    // The interval in seconds to re-assert the execution state to prevent sleep.
    // The recommended value is 60 seconds. The Windows API itself is what prevents sleep,
    // this timer just ensures the state is maintained periodically.
    "UpdateIntervalSeconds": 60
  }
}
